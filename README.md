# csrf-attack-simulation-seed-lab
This project demonstrates Cross-Site Request Forgery (CSRF) attacks using both GET and POST methods against the Elgg web application in a controlled SEED Lab environment.  The lab also shows how CSRF protection mechanisms (secret tokens) prevent these attacks.
